@base = http://localhost:5000

###
# Login to get Bearer Token
# Copy the token from the response and use it in other requests
POST {{base}}/api/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "admin"
}

###
# The response will look like:
# {
#   "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
#   "expiresIn": 3600,
#   "tokenType": "Bearer"
# }
#
# Copy the token value and use it in the @token variable below
# Or use the REST Client extension's automatic token capture feature

###
# Set this token after logging in
# Replace with actual token from login response
@token = your_token_here

###
# Example: Test authenticated request
GET {{base}}/api/tables
Authorization: Bearer {{token}}

###
